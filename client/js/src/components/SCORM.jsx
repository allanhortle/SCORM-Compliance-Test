/** @jsx React.DOM */
var React = require('react');
var _ = require('lodash');

var SCORM = React.createClass({
    displayName: 'SCORM',
    render: function () {
        return (
            <div>SCORM</div>
        );
    }
});

module.exports = SCORM;
